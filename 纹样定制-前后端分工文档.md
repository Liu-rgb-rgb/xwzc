# AI 纹样定制 —— 前后端分工文档

> 目标：把 **AI 生成的纹样** 应用到 **文创商品** 上，生成一张真实的定制预览图，保存为定制方案，最终可下单。
>
> 方案：**前端 Canvas 合成**（用户在浏览器里缩放 / 拖动 / 旋转纹样，实时预览，满意后导出图片上传给后端保存）。
>
> 本文档定义 **前后端各自的职责、接口契约、配置依赖、联调验收**，作为协作依据。

---

## 1. 整体流程（数据流）

```
[前端] 用户进入定制页 /customize，选择 商品 + 纹样
   │
   ▼
[前端] Canvas 实时合成：商品底图 + AI 纹样（可 拖动 / 缩放 / 旋转）
   │
   ▼
[前端] 用户点击「生成定制预览」
   │
   ▼
[前端] canvas.toBlob() 导出合成图（PNG）
   │
   ▼
[前端 → 后端]  POST /api/files/upload      （multipart: file）
[后端 → 前端]   返回 { fileUrl: "https://xwzc.oss.../CUSTOM/xxx.png" }
   │
   ▼
[前端 → 后端]  POST /api/custom-designs
               { productId, patternId, designConfig, previewImageUrl, remark }
   │
   ▼
[后端] 保存 custom_design（previewImageUrl 用前端传的真实合成图）
   │
   ▼
[前端] 「我的定制作品」展示 previewImageUrl
   │
   ▼
[前端 → 后端]  下单 createOrder（已支持 customDesignId）
   │
   ▼
[商家端] 商家接单，按 patternId + designConfig 制作
```

**核心原则**：图片合成都发生在 **前端浏览器** 里；后端只负责 **接收、存储、返回** 前端合成的成品图 URL。后端 **不做** 任何图片叠加处理。

---

## 2. 明确分工

### 2.1 前端负责

| # | 事项 | 说明 |
|---|---|---|
| F1 | 新增组件 `PatternComposer.vue` | Canvas 合成核心：底图=商品图，图层=纹样，支持 **拖拽(x,y) / 缩放(scale) / 旋转(rotation)**，实时重绘 |
| F2 | 图片跨域加载 | 所有 `<img>` 加载 OSS 图时必须设 `img.crossOrigin = 'anonymous'`（否则 Canvas 导不出，见 §5） |
| F3 | 纹样选择控件 | 定制页当前 **只有选商品**，需补一个选纹样的控件。数据源：`localStorage` 里的 `saved_patterns` + `recent_generations`（AI 生成的）+ 内置 `patterns` |
| F4 | 导出合成图 | `canvas.toBlob(blob => ...)` 导出 PNG |
| F5 | 上传合成图 | 调 `POST /api/files/upload`，`file` 字段 = 上面的 blob，拿到 `fileUrl` |
| F6 | 创建定制 | 调 `POST /api/custom-designs`，body 带 `previewImageUrl`（= F5 拿到的 URL）和真实 `designConfig` |
| F7 | 展示合成图 | 「我的定制作品」卡片用 `design.previewImageUrl` 展示（**不再是商品原图**） |

### 2.2 后端负责

| # | 事项 | 说明 |
|---|---|---|
| B1 | **新增**用户端上传接口 `POST /api/files/upload` | 前端 `api.ts` 已按此路径写好，但后端目前只有 `/api/admin/files/upload`（商家端、要 `bizType`）。需新增 C 端可用版本，见 §4.1 |
| B2 | `CustomDesignDTO` 加字段 `previewImageUrl` | 见 §4.2 |
| B3 | `createDesignDetail` 支持 `previewImageUrl` | 签名加参数；实现里 **传了就用前端的，没传才走 mock**（向后兼容） |
| B4 | Controller 透传 `dto.getPreviewImageUrl()` | |
| B5 | 确认商品/纹样数据接口可用 | 前端要拿到商品图（`coverImage`/`image`）和纹样 id + 图 |

### 2.3 共同 / 运维

| # | 事项 | 负责人 |
|---|---|---|
| C1 | **OSS 跨域（CORS）配置** | 阿里云控制台操作（后端同学配合，见 §5）。**前端 Canvas 导出强依赖** |
| C2 | 接口契约对齐 | 双方按本文档 §4 |

---

## 3. 数据从哪里来（前端取图）

| 数据 | 来源 | 图字段 | id 字段 |
|---|---|---|---|
| 商品 | `api.products.*`（`/api/products`） | `image` / `coverImage` | `id`（= productId） |
| 纹样（AI 生成） | 前端 `localStorage`：`saved_patterns`、`recent_generations`（由生成页写入） | `image`（对应后端 `pattern.imageUrl`） | `id`（= patternId） |

> ⚠️ 前端选纹样时，**只允许选「已生成过 / 已保存」的纹样**，确保 `patternId` 在后端真实存在（后端创建定制时会用它关联、并在详情里反查纹样图）。

---

## 4. 接口契约（重点，前后端以此为准）

### 4.1 文件上传（后端需新增）

- **`POST /api/files/upload`**
- `Content-Type: multipart/form-data`
- **鉴权**：需登录（请求头 `Authorization: Bearer <token>`，前端 axios 已自动带）

**请求参数**

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `file` | File | 是 | 前端导出的 PNG（canvas 合成图） |

> 前端 **只传 `file`**，不传 bizType；后端固定按业务目录存（建议 `CUSTOM`）。

**响应**（`Result<FileResource>`）

```json
{
  "code": 200,
  "message": "ok",
  "data": {
    "id": 123,
    "fileUrl": "https://xwzc.oss-cn-beijing.aliyuncs.com/CUSTOM/6f2a...png",
    "originalName": "preview.png",
    "fileSize": 123456,
    "mimeType": "image/png",
    "bizType": "CUSTOM"
  }
}
```

> 前端取 `data.fileUrl` 作为 `previewImageUrl`。

---

### 4.2 创建定制方案（后端需改）

- **`POST /api/custom-designs`**
- `Content-Type: application/json`
- **鉴权**：需登录

**请求体**

```json
{
  "productId": 501,
  "patternId": 301,
  "designConfig": { "x": 0.5, "y": 0.48, "scale": 0.75, "rotation": 0 },
  "previewImageUrl": "https://xwzc.oss-cn-beijing.aliyuncs.com/CUSTOM/6f2a...png",
  "remark": "用户定制要求备注"
}
```

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `productId` | Long | 是 | 商品 id |
| `patternId` | Long | 是 | 纹样 id |
| `designConfig` | **JSON 对象** | 否 | 前端合成参数，后端自动序列化为 JSON 字符串存储（已有 `JsonObjectToStringDeserializer`，**前端直接传对象即可**） |
| `previewImageUrl` | String | 否 | **前端合成的真实成品图 URL**（本次新增）。不传时后端回退到 mock 占位图 |
| `remark` | String | 否 | 备注 |

**响应**（`Result<CustomDesignVO>`）

```json
{
  "code": 200,
  "data": {
    "id": 17,
    "productId": 501,
    "patternId": 301,
    "previewImageUrl": "https://xwzc.oss-cn-beijing.aliyuncs.com/CUSTOM/6f2a...png",
    "designConfig": "{\"x\":0.5,\"y\":0.48,\"scale\":0.75,\"rotation\":0}",
    "remark": "用户定制要求备注",
    "status": "NORMAL",
    "productName": "牡丹帆布袋",
    "productCoverImage": "https://.../product.jpg",
    "patternTitle": "牡丹 · 广绣经典",
    "patternImageUrl": "https://.../pattern.png"
  }
}
```

> 「我的定制作品」前端用 `data.previewImageUrl` 展示。

---

### 4.3 `designConfig` 字段约定（合成参数，前后端统一口径）

| 字段 | 类型 | 取值范围 | 含义 |
|---|---|---|---|
| `x` | number | `0 ~ 1` | 纹样中心点相对画布宽度的横向位置（`0.5` = 水平居中） |
| `y` | number | `0 ~ 1` | 纹样中心点相对画布高度的纵向位置（`0.5` = 垂直居中） |
| `scale` | number | `0.1 ~ 2.0` | 纹样缩放比例（相对商品主体区域的基准尺寸） |
| `rotation` | number | `0 ~ 360` | 纹样旋转角度（度，顺时针） |

> **说明**：`designConfig` 主要由 **前端 Canvas 使用**，后端 **只存储、不解析**（供商家端查看 / 制作参考）。因此只要前后端字段名和范围一致即可，具体像素换算由前端负责。

---

## 5. OSS 跨域（CORS）配置 —— 前端 Canvas 导出的强依赖 ⚠️

**为什么必须配**：Canvas 里画了跨域（OSS）图片后再 `toBlob()` / `toDataURL()`，若 OSS 未返回 CORS 头，浏览器会判定画布被「污染」，导出直接抛 `SecurityError`，整个功能白做。

**配置位置**：阿里云 OSS 控制台 → 桶 `xwzc` → 权限管理 → **跨域设置 (CORS)** → 创建跨域规则。

**填法（推荐，最省事）**

| 字段 | 值 |
|---|---|
| 来源 | `*` |
| 允许 Methods | 勾 **GET** |
| 允许 Headers | 留空 |
| 暴露 Headers | 留空 |
| 缓存时间(秒) | `0` |
| 返回 Vary: Origin | **不勾** |

- 规则约 **15 分钟**内生效。
- 若不想用 `*`：来源填 `http://localhost:5173`（生产再补线上域名），同样只勾 GET。
- 配完后，前端加载图片 **必须** `img.crossOrigin = 'anonymous'`，两者配合才有效。

> 注：CORS 只影响 JS/Canvas 跨域使用，**不影响**浏览器地址栏直接打开图片。

---

## 6. 前端 Canvas 实现要点（给前端同学）

1. **画布尺寸**：建议固定正方形（如 `600 × 600`），CSS 等比缩放显示。
2. **图片加载**：
   ```js
   function loadImage(src) {
     return new Promise((resolve, reject) => {
       const img = new Image();
       img.crossOrigin = 'anonymous';   // ★ 必须
       img.onload = () => resolve(img);
       img.onerror = reject;
       img.src = src;
     });
   }
   ```
3. **绘制顺序**：
   - 先画商品底图（按 contain 居中铺满画布）；
   - 再画纹样：`ctx.translate(cx, cy); ctx.rotate(rad); ctx.scale(s, s); ctx.drawImage(pattern, -w/2, -h/2, w, h);`，其中 `cx=canvasW*x`、`cy=canvasH*y`、`w=baseW*scale`。
4. **交互**：
   - `mousedown` + `mousemove` → 更新 `x, y`（换算成 0~1）；
   - `wheel` 或 `<input type=range>` → 更新 `scale`（建议 0.1~2）；
   - `<input type=range>` → 更新 `rotation`（0~360）。
   - 每次变更后重绘画布。
5. **导出**：
   ```js
   canvas.toBlob((blob) => {
     const fd = new FormData();
     fd.append('file', blob, 'preview.png');
     // → POST /api/files/upload → 拿 data.fileUrl
     // → POST /api/custom-designs（带 previewImageUrl: fileUrl, designConfig: {x,y,scale,rotation}）
   }, 'image/png');
   ```
6. **纹样背景**：AI 纹样自带背景，直接叠加会脏。二选一（见 §7 待确认项）：
   - 生成纹样时 prompt 加「透明背景 / 纯色背景」；
   - 或 Canvas 里给纹样图层加圆形 / 矩形裁切蒙版。

---

## 7. 待确认项（需产品 / 前后端拍板）

1. **纹样背景处理方式**：prompt 加透明背景，还是 Canvas 蒙版裁切？（影响生成效果 + 前端实现）
2. **画布/商品模板**：商品是否需要「模板图 + 纹样放置区域」？MVP 可先用商品图 + 固定居中规则。
3. **上传目录**：合成图存 OSS 的哪个目录？建议 `CUSTOM`。

---

## 8. 后端改动清单（后端同学照此实现）

| # | 文件 | 改动 |
|---|---|---|
| B1 | 新增 `controller/user/FileUploadController.java`（或复用/拆分现有 `FileController`） | `POST /api/files/upload`：接收 `file`，内部固定 `bizType="CUSTOM"`，调 `ossFileService.upload(file, "CUSTOM")`，存 `FileResource`，返回 `Result<FileResource>` |
| B2 | `product/dto/CustomDesignDTO.java` | 加字段 `private String previewImageUrl;` |
| B3 | `product/service/CustomDesignService.java` | `createDesignDetail` 方法签名加参数 `String previewImageUrl` |
| B3 | `product/service/impl/CustomDesignServiceImpl.java` | `previewImageUrl` 非空 → `design.setPreviewImageUrl(previewImageUrl)`；为空 → 保留现有 mock 逻辑 |
| B4 | `web/controller/user/CustomDesignUserController.java` | `createDesign` 调用时透传 `customDesignDTO.getPreviewImageUrl()` |

---

## 9. 联调 & 验收清单

**联调顺序建议**：先配好 CORS（§5）→ 后端完成 B1~B4 并自测接口 → 前端完成 F1~F7 → 联调。

- [ ] CORS 已配：前端能正常 `toBlob` 导出，**不报** `SecurityError`
- [ ] `POST /api/files/upload` 能上传 PNG 并返回 `data.fileUrl`
- [ ] `POST /api/custom-designs` 带 `previewImageUrl` 时，落库的是真实合成图（非 mock）
- [ ] 创建定制响应 `data.previewImageUrl` = 前端上传的那张图
- [ ] 「我的定制作品」显示的是 **合成后** 的图（纹样已叠在商品上），可缩放痕迹可见
- [ ] 从「我的纹样 / AI 生成」选纹样 → 定制 → 下单 全流程走通
- [ ] 商家端能看到该定制方案（patternId + designConfig + 预览图）

**常见坑**

| 现象 | 原因 | 处理 |
|---|---|---|
| `toBlob` 抛 `SecurityError` | OSS 没配 CORS 或图片没设 `crossOrigin` | 见 §5，前端加 `crossOrigin='anonymous'` |
| 上传 404 | 前端调 `/api/files/upload` 但后端没实现 | 后端完成 B1 |
| 落库预览图是 mock 时间戳 URL | `previewImageUrl` 没传到后端 | 检查 B2~B4 是否透传 |
| 纹样叠上去很脏 | 纹样带背景 | 见 §6.6 / §7.1 |

---

## 10. 一句话总结

- **前端**：做 Canvas 缩放合成 + 纹样选择 + 导出上传 + 传 `previewImageUrl`。
- **后端**：新增 C 端上传接口 + 创建定制接口接收 `previewImageUrl`（共 4 处小改）。
- **共同**：配好 OSS CORS（前端导出的命门）。
