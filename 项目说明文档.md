# 绣纹智创 · 项目说明文档（技术版）

> 非遗广绣 AI 纹样设计与文创定制平台 —— 技术架构与实现说明
> 面向技术评审、团队协作与演示支撑

---

## 一、系统概述

**绣纹智创**是一套前后端分离的非遗广绣数字化平台，采用 Spring Boot 多模块后端 + Vue3 前端，支持**用户端**与**商家端**两类角色，实现 AI 纹样生成、文创定制、电商交易、非遗教学的一体化闭环。

- 项目代号：`xwzc`（绣纹智创）
- 后端仓库：`xiuwen-backend-framework`（Maven 聚合工程）
- 前端仓库：`vue3-frontend`（Vite + Vue3 + TypeScript）

---

## 二、技术栈

### 2.1 后端

| 技术 | 版本/说明 |
|------|----------|
| 语言 | JDK 8 |
| 框架 | Spring Boot 2.7.x |
| ORM | MyBatis-Plus（逻辑删除、分页、代码生成） |
| 数据库 | MySQL 8.0（utf8mb4，19 张业务表） |
| 缓存/锁 | Redis + Redisson（分布式锁） |
| 鉴权 | JWT（无状态登录） |
| 对象存储 | 阿里云 OSS |
| AI 生图 | 阿里云百炼 Qwen-Image 3.0（兼容 OpenAI/DALL-E 切换） |
| 构建 | Maven 多模块聚合 |

### 2.2 前端

| 技术 | 说明 |
|------|------|
| 框架 | Vue 3 |
| 语言 | TypeScript |
| 构建 | Vite |
| 路由 | Vue Router 4 |
| HTTP | Axios |
| 代码规范 | Prettier + ESLint |

### 2.3 基础设施

- 版本协同：Git（多分支协作开发）
- 接口调试：Apifox
- 文件托管：阿里云 OSS（CDN 加速）

---

## 三、后端工程架构（多模块）

```
xiuwen-backend-framework
├── xiuwen-common      通用模块：返回结果、分页、常量、异常、工具类
├── xiuwen-framework   框架模块：MyBatis-Plus、JWT、拦截器、跨域、\n│                       全局异常、OSS、AI、分布式锁
├── xiuwen-system      系统模块：用户、店铺、地址、消息、文件、首页配置
├── xiuwen-pattern     纹样模块：AI 纹样生成、纹样库、提示词模板
├── xiuwen-product     商品模块：文创商品、分类、定制设计、购物车
├── xiuwen-order       订单模块：订单主表、订单明细
├── xiuwen-course      课堂模块：非遗课程、课程分类、创作资源
└── xiuwen-web         启动模块：启动类 + 用户端/商家端 Controller
```

**模块依赖方向**：`web → (system / pattern / product / order / course) → framework → common`，业务模块之间低耦合，通过 `web` 层编排。

---

## 四、数据库设计

数据库 `xwzc`，共 **19 张表**，全部使用 utf8mb4 编码、InnoDB 引擎、逻辑删除字段 `deleted`，字段均带中文 COMMENT。

| # | 表名 | 说明 |
|---|------|------|
| 1 | user | 用户账号表（普通用户/商家管理员/平台管理员统一存储） |
| 2 | shop_info | 店铺信息表（单商家平台） |
| 3 | user_address | 用户收货地址表 |
| 4 | message_notice | 消息通知表 |
| 5 | pattern_generation | AI 纹样生成记录表 |
| 6 | pattern | 纹样表（生成/保存/收藏/推荐） |
| 7 | prompt_template | AI 提示词模板表 |
| 8 | product_category | 文创商品分类表 |
| 9 | product | 文创商品表 |
| 10 | custom_design | 商品定制设计表（纹样套用商品的预览记录） |
| 11 | cart_item | 购物车表 |
| 12 | orders | 订单主表 |
| 13 | order_item | 订单明细表 |
| 14 | course_category | 非遗课程分类表 |
| 15 | course | 非遗课堂课程表 |
| 16 | learning_resource | 创作资源/学习资料表 |
| 17 | file_resource | 文件资源表（头像/商品图/纹样图等统一管理） |
| 18 | home_banner | 首页 Banner 表 |
| 19 | home_recommend | 首页推荐内容表 |

### 关键设计说明

- **角色模型**：`user.role` 区分 `USER`（普通用户）/ `MERCHANT_ADMIN`（商家管理员）/ `ADMIN`（平台管理员），单用户表复用，简化权限体系。
- **订单状态机**：`orders.status` 从 `WAIT_PAY → WAIT_CONFIRM → PRODUCING → WAIT_DELIVERY → DELIVERED → COMPLETED`，外加 `CANCELLED`，每个状态流转都会记录对应时间戳字段（`paid_at` / `confirmed_at` / `produced_at` / `shipped_at` / `completed_at`）。
- **定制闭环**：`pattern`（纹样）→ `custom_design`（套用到商品的预览）→ `cart_item` → `orders` / `order_item`，形成完整定制链路。
- **快照机制**：订单明细（`order_item`）冗余存储商品名、图片、单价快照，避免商品后续修改/删除影响历史订单。

---

## 五、功能模块详解

### 5.1 用户端

| 模块 | 说明 |
|------|------|
| 首页 | Banner、推荐内容聚合 |
| 认证 | 注册、登录（JWT） |
| AI 纹样生成 | 关键词 → 风格/颜色/场景 → 生成纹样 |
| 纹样库 | 浏览、收藏、我的纹样 |
| 文创商品 | 商品列表、详情、定制 |
| 购物车 | 加购、改数量、选中结算 |
| 订单 | 下单、支付(mock)、取消、确认收货、状态统计 |
| 非遗课堂 | 课程分类、课程列表、详情、学习 |
| 创作资源 | 资源列表、详情、下载 |
| 个人中心 | 资料、头像、收货地址 |
| 消息 | 站内消息、订单通知 |
| 搜索 | 全局搜索 |

### 5.2 商家端

| 模块 | 说明 |
|------|------|
| 数据看板 | 经营数据统计 |
| 订单管理 | 列表、详情、状态流转、商家备注、状态统计 |
| 商品管理 | 商品增删改查、上下架 |
| 定制管理 | 定制设计记录管理 |
| 纹样管理 | 纹样审核、推荐、上下架 |
| 生成记录 | 用户 AI 生成记录查询 |
| 提示词模板 | 模板管理 |
| 课程管理 | 课程、分类管理 |
| 资源管理 | 创作资源管理 |
| 用户管理 | 用户账号管理 |
| 店铺管理 | 店铺资料维护 |
| 消息管理 | 系统消息推送 |
| 首页管理 | Banner、推荐内容配置 |

---

## 六、核心技术创新

### 6.1 AI 纹样生成（Qwen-Image 3.0）

- 位于 `xiuwen-framework/service/AiImageService`。
- 通过 `xiuwen.ai.provider` 可切换 `qwen`（默认）与 `openai`（DALL-E / 中转）。
- **Qwen 模式**：调用阿里云百炼多模态生成接口，模型 `qwen-image-3.0`，开启 `prompt_extend` 让大模型自动扩写提示词提升画质；每次生成 1 张，按数量循环调用，返回图片 URL 下载为字节再走 OSS 上传链路。
- 支持多种风格（广绣经典 / 新中式 / 刺绣风）、颜色主题、应用场景、参考图。

### 6.2 纹样 → 商品定制闭环

- 生成/收藏的纹样可套用到可定制商品底图（`mockup_image`），生成定制预览图（`custom_design`）。
- 预览图、纹样图均通过 OSS 存储，返回完整 URL 供前端渲染。

### 6.3 分布式锁防超卖

- 位于 `xiuwen-framework/lock/StockLockService`。
- 基于 **Redisson RLock**，商品扣减/回补串行化；多商品场景通过 `RedissonMultiLock` 一次性原子加锁，并按 `productId` 排序防止死锁。
- 锁超时兜底（lease 30s）、等待超时（3s）后让用户重试，保证并发下单时库存正确。

### 6.4 阿里云 OSS 对象存储

- 图片存储从本地文件系统迁移至 OSS，图片地址统一为 OSS URL，支持 CDN 加速。
- `OssFileService` 提供 `upload()` 与 `getOssDomain()`，商品图、纹样图、头像、轮播图等统一走 OSS。

### 6.5 JWT 无状态鉴权

- 用户端与商家端通过 JWT Token 鉴权，拦截器校验角色与登录态。
- 商家端接口路径 `/api/admin/**` 需 `MERCHANT_ADMIN` / `ADMIN` 角色。

---

## 七、环境部署

### 7.1 环境要求

| 项 | 要求 |
|----|------|
| JDK | 8+ |
| Maven | 3.6+ |
| MySQL | 8.0 |
| Redis | 5.0+（Redisson 分布式锁依赖） |
| Node.js | 18+（前端） |
| pnpm | 前端包管理 |

### 7.2 后端启动

1. 执行数据库脚本 `xiuwen_zhichuang_v1_schema.sql`（自动建库建表 + 测试数据）。
2. 修改 `xiuwen-web/src/main/resources/application.yml`：
   - MySQL 账号密码；
   - Redis 连接；
   - 阿里云 OSS 的 `access-key-id` / `access-key-secret` / `bucket-name` / `domain`；
   - AI 的 `qwen-api-key`。
3. 构建并启动：

```bash
mvn clean package
cd xiuwen-web
mvn spring-boot:run
```

后端默认端口 `8080`。

### 7.3 前端启动

```bash
cd vue3-frontend
pnpm install
pnpm dev
```

前端默认端口 `5173`（Vite）。

### 7.4 默认账号

| 角色 | 账号 | 密码 |
|------|------|------|
| 商家管理员 | shopadmin | 123456 |
| 测试商家 | testmerchant | merchant123 |
| 普通用户 | 自行注册 | — |

> 当前版本密码使用 MD5 存储，正式上线建议切换为 BCrypt。

---

## 八、接口规范

- 统一返回结构 `Result<T>`：

```json
{ "code": 200, "message": "success", "data": { } }
```

- 用户端接口前缀 `/api/**`，商家端接口前缀 `/api/admin/**`。
- 分页参数：`page` / `pageSize`，返回 `total` / `list`。
- 错误码：200 成功、400 业务异常、401 未登录、403 无权限、500 系统异常。

---

## 九、项目特色总结

1. **非遗垂直 + AI 创新**：不做泛化国风，专攻广绣语境，提示词模板与纹样库高度垂直。
2. **全链路闭环**：AI 创作 → 定制 → 电商 → 教学，一个平台全搞定。
3. **工程化可靠**：分布式锁、OSS、JWT、逻辑删除、多模块架构，具备生产级工程素养。
4. **协同开发**：Git 分支协作 + Apifox 接口管理，多人高效并行。

---

## 附录

- 数据库脚本：`xiuwen_zhichuang_v1_schema.sql`
- 后端模块说明：`xiuwen-backend-framework/README.md`
- 变更记录：`商家端订单模块修改记录.md`、`OSS迁移变更记录.md`
- 前端结构：`vue3-frontend/src/pages/`（用户端页面）+ `pages/admin/`（商家端页面）